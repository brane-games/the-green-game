extends Node

export (Array, Texture) var textures
export (Array, Texture) var textures2
export (NodePath) var mesh
export (NodePath) var mesh2

# Called when the node enters the scene tree for the first time.
func _ready():
	pass
#	assert(textures.length() > 0)


func change_color():
	pass
